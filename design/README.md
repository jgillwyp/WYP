# WYP — Design

Static HTML mockups for every app screen, in palette 1. These are the design
source of truth: a screen is designed here first, reviewed here, then converted
to a React component under `app/`.

**This folder is not served.** It sits outside `public/`, so nothing here is
reachable from the live Vercel URL. It is also in `tsconfig.json`'s `exclude`
list, so nothing here is typechecked or can break a build.

## Folders

| Folder | Holds |
|---|---|
| `marketing/` | Public-facing sales/landing pages — a different category from the app screens below (full-width, desktop-and-mobile responsive, no 480px `.frame-none`, no palette-1 component classes). New 2026-08-12, one file so far |
| `screens/` | Canonical, approved mockup for each screen — one file per screen. Also holds `tokens.css`/`components.css` (regenerated 2026-08-12 from `app/globals.css`) — a reference for keeping each mockup's own embedded `<style>` in sync by hand; no mockup actually links either file (see "Notes" below) |
| `drafts/` | Variants under discussion, and superseded work kept for reference |
| `spec/` | The UI spec the `§` comments in each mockup refer to |

## Screen map

| Screen | Mockup | Route | Status |
|---|---|---|---|
| Main (Requests + ToDos) | `screens/WYP_main_screen_palette1.html` | `/` | **Live** (`app/components/MainScreen.tsx`, 2026-08-08, extended 2026-08-09 — the first working landing screen after sign-in). Sent and ToDos are real `requests` rows (RLS-scoped to the signed-in owner, no client-side filter needed): Sent defaults to Due Date descending, ToDos to Priority ascending, both matching the mockup's `Due ▼`/`Priority ▼` sort pills. Clicking a Sent row opens Request Detail (`/requests/[id]`); a ToDo row opens ToDo Detail (`/todos/[id]`) — 2026-08-09; both return here via `router.back()` rather than a fresh `push('/')`, so Next's back-navigation scroll restoration lands back where the row was, not at the top — owner-reported: "When I edited a ToDo I was returned to the top of the main screen instead of to where I have edited the ToDo." Main Screen still fully remounts and refetches on the way back (this app has no Cache Components/Activity — see CLAUDE.md), so the edited row's new data shows correctly. **Chip state now survives the round trip too** (2026-08-09 — "It would be appropriate to return to the same chip state on the main screen"): Sent filter, ToDos filter, and Housekeeping's Tasks/How-to Videos tab are written to `sessionStorage` on every change and read back on mount, since plain `useState` alone was lost on each remount. Scoped to the chips themselves — the search text box is not persisted (a scoping call, not confirmed with the owner). `sessionStorage`, not `localStorage`: a within-session view preference, not a durable account setting, so it's fine for it to reset once the tab actually closes. **Received is now live too (2026-08-11, migration 012)** — superseding the placeholder note that used to be here. `get_received_requests()` matches the signed-in caller's own session email against the sending Contact's email, server-side (no schema change needed for the match itself — `requests` RLS stays owner-only, migration 003; there's still no column linking a row to its recipient's own account, which is exactly why this needed a function rather than a plain select). Same `.reqrow`-style rows as Sent, same All/Open/Overdue/Done chips, routes to Response Detail (`/requests/[id]/respond`) rather than Request Detail. Self-sent Requests are not excluded — see decisions log. **Filter chips and search are now functional on all three sections** (2026-08-09, extended to Received 2026-08-11): All/Open/Overdue/Done on Sent and Received, All/Open/Done on ToDos, filtering the already-fetched rows client-side (ToDos defaults to Open selected, matching the mockup); the search field matches description plus contact-name (Sent) / owner-name (Received) / category (ToDos), case-insensitive substring, across all three sections at once; the scope button ("All ▼") stays visual-only — no designed picker exists and there's nothing yet for a scope to narrow. Housekeeping: "Contacts" navigates to `/contacts` (renamed from "My Contacts" the same day, see that screen's own entry below); "Account" (renamed from "Your Account", then from "My Account" — both 2026-08-09, see decisions log) stays inert — that screen is intentionally undesigned pending further product evolution. The owner's rule, stated directly: a Housekeeping row's label must repeat exactly what its destination screen is titled once you're there. Log Out is real (`supabase.auth.signOut()` + redirect to `/login`). Housekeeping's "Tasks"/"How-to Videos" two-chip toggle (2026-08-08 mockup change) ported to React state. Icons are inline SVG (currentColor), not the mockup's base64 PNGs — matches every other converted screen; shapes adapted from the canonical `wyp_icon_*.svg` source except Print, which reuses the existing icon from Create Request/Request Response. A one-time seed script (Contacts + Sent Requests + ToDos, `docs/Week2 - SQL history.txt`, confirmed run by the owner 2026-08-11) gives the live Sent/ToDos sections believable demo data — Received was deliberately not seeded (it wasn't live yet at the time) and instead now reads whatever real Requests happen to name the signed-in owner's own email as a Contact. **To/From column-gap tightened, 2026-08-11** — owner-reported: a long contact first name ("Maximillan") truncated to "Maximilla…" on a phone. Font was never the issue (`.dt`/`.due`/`.dn` already use the app's one font, Inter, proportional the whole time — no monospace declaration to remove); the constraint was `.r1`/`.colbar.sr`'s `grid-template-columns: 1fr 58px 58px 58px` reserving 48px across three 16px gaps. `column-gap` cut to `10px` in both (must move together — the header row's labels sit directly above the data columns), handing the freed 18px to the name column's `1fr` track; the 58px date-column widths themselves are untouched, already close to their content's minimum. Ported into `WYP_main_screen_palette1.html`'s own embedded `<style>` block. **Column-header sorting is now live, 2026-08-11** — owner: "Please work on the column heading sorting for the three sections of the Main screen." Every `.colbar` cell (To/From, Date, Due, Done on Sent/Received; Priority, Category — Description on ToDos) is a real button now, not static text — click to sort by that column in its own default direction, click again to reverse. The active column shows the existing `.pill` treatment (previously a fixed, non-interactive default on Due/Priority only) with its label plus a ▲/▼ arrow. Sort state persists per-section to `sessionStorage` (`wyp.mainSentSort` / `wyp.mainReceivedSort` / `wyp.mainTodoSort`), same tier and pattern as the filter chips (2026-08-09). Nulls always sort last regardless of direction. `aria-sort` isn't valid on a plain `<button>` (role mismatch, caught by `eslint`'s `jsx-a11y/role-supports-aria-props`) — used `aria-label` instead to convey the same state. See decisions log for the full write-up, including the rejected fixed-direction alternative. **Mockup unchanged** — `WYP_main_screen_palette1.html`'s `.colbar` cells are static `<span>`s with no click handlers, same "static demo" scoping as the filter chips and search box before they went live-only. **ToDos gained an Overdue chip, 2026-08-12** — owner: "Now that ToDos have Due and Done Dates, we need to add the Overdue chip for ToDos to match the chips order for Requests Sent and Received." ToDos' filter chips are now All/Open/Overdue/Done, same order as Sent/Received; `todoStatus()` reuses the same `statusFor(due_date, done_date)` helper Sent/Received already used, so a ToDo with no Due Date can never read as Overdue — it just falls through to Open/Done, exactly the owner's own scoping ("ToDos without a Due Date would be ignored for the Overdue chip"). The ToDo row itself now also carries `.overdue`/`.done`, recoloring `.pri`/`.cat`/`.tdd` (ToDo's own text classes, since a ToDo row is one flowing line, not Sent/Received's four-column grid) the same way `.row.overdue`/`.row.done` already recolor `.nm`/`.dt`/`.due`/`.dn`/`.desc` — confirmed with the owner directly: "The overdue items in the ToDo list should follow the red-display of text to match the Requests." **The Expand icon is gone, app-wide, same day** — owner: the icon's whole premise (each section has its own limited "elevator," Expand opens the rest full-screen) doesn't match how Sent/Received/ToDos actually ship (every selected item already shows under its own section, which he also judged the practically correct behavior) — and an expanded view's only remaining value, more lines of Description, has minimal utility on its own. Removed from `MainScreen.tsx` (all three `subicons` clusters, plus the now-dead `ExpandIcon()` component) and from this mockup (all three icon `<span>`s); Contract was never built anywhere, so there was nothing to remove for it. Print's icon and behavior are unaffected. **Leaves the PRD (§3.7) and UI spec (§8.7, §9.7, §5.1/§11.1) documenting a feature the app no longer offers** — flagged to the owner as a separate, not-yet-decided follow-up rather than silently left inconsistent; see the decisions log's 2026-08-12 entry. **Scroll position now survives Add/Edit ToDo, 2026-08-13** — the `.scroll` div is internally-scrolling, not the window, so the 2026-08-09 `router.back()` fix above never actually restored it; fixed with explicit `sessionStorage` persistence (`wyp.mainScrollTop`), plus `CreateTodoForm.tsx`'s Save/Cancel switched from `push('/')` to `back()` to match every other screen. **Print Reports, 2026-08-13** (§6.34 PROPOSED, not drawn in any mockup) — owner: the Print buttons printed the live, internally-scrolling on-screen layout as-is, "only shows what can fit onto a page." Replaced with a dedicated print-only report per section, built from the owner's own xlsx mockup and shown only for the duration of a print (`.print-report`/`.no-print`, `@media print`) — full untruncated descriptions, the existing Dialog icon, a Due Time sub-line when set (Sent via a plain column, Received via new migration 017 extending `get_received_requests()`), and, for ToDos specifically, a different column set than the on-screen row (Description/Due/Done, not Priority/Category) per the owner's own mockup. Sourced from `sortedSent`/`sortedReceived`/`sortedTodos` — the owner confirmed: "The print should follow the chip and sort set for the section by the user." Migration 017 confirmed run 2026-08-13. **ToDos' Category column is now conditional, same day** (migration 018, `profiles.private_category_enabled`, off by default — see the new Account row below) — the colbar drops to plain "Description" text (no longer clickable) and each row drops its `.cat` span when off; unaffected when on. Mockup unchanged — it has no interactive Category-visibility JS to update. **Print Sent/Received's Due Time sub-line is now conditional too** (migration 019/020/021, confirmed run by the owner 2026-08-13) — Sent gated by the signed-in owner's own `request_time_enabled`, Received gated per-row by each row's own `owner_request_time_enabled` (a different sender may have this on or off) since it's not a single account-wide flag the way Sent's is. No mockup — this is a print-only layout with nothing to update. **Attachments icon added, 2026-08-14** (Week 5 Priority 3) — a paperclip icon (`AttachmentIcon`) renders next to the existing Dialog icon, both live rows and Print Reports, whenever a Sent/Received row has at least one real file attachment; Sent reads it via a plain `attachments(count)` PostgREST embed (same technique as `dialog(count)`), Received via migration 027 extending `get_received_requests()` (confirmed run by the owner 2026-08-14). ToDos' own Locations don't get a print/row icon yet — deferred, not forgotten |
| Start Free Account / Sign In | `screens/WYP_signin_palette1_floating.html` | `/login` | Converted, **needs update** (React page lacks the two modes; title shortened from "Start my Free Account" 2026-08-07) |
| Create Free Account — first run | `screens/WYP_create_free_account_palette1.html` | `/account/new` | **Live** (`app/components/CreateFreeAccountForm.tsx`, 2026-08-11) — superseding the "not reachable" note below. Owner asked to build it for limited testing. Not a sign-up screen and never was: Email renders `.metarow` read-only from the session, and the mockup's own footer copy ("No password on this account... you have already signed in with the one-time link") already said so — no conflict with CLAUDE.md's magic-link-only Auth section, since the account and its stub `profiles` row (via `handle_new_user`) already exist by the time this screen is reached. Fields: First/Last/Display Name and Time Zone (`.req`, lookup — same pattern as Add Contact/Contact Detail), Phone (`.opt`), Notify Me By (Email selected, Text `.is-locked`, subscription gate). Save-only band, no Cancel — matches the mockup's own reasoning (an exit here would strand an authenticated account with no completed profile). Writes via UPDATE, not insert. **Wired as the mandatory first-run step**: `app/auth/callback/page.tsx` now checks `profiles.display_name` after a successful sign-in and routes here when it's null, completing the exact behavior the Week 1 schema comment on that column already described as the plan. **Depended on migration 013** (grant `UPDATE(time_zone)` on `profiles` to `authenticated`) — **confirmed run by the owner, verified 2026-08-12** via a direct `information_schema.column_privileges` query, after the Supabase SQL editor's own History view was reported showing entries only through migration 011 and couldn't be trusted on its own. A real bug had been found while scoping this: the column-specific UPDATE grant from Week 1 predates `time_zone` (added in migration 007) and was never extended to include it, so every write to `profiles.time_zone` — including Add Contact/Contact Detail's own browser-detected fallback — had been silently failing since migration 007. Save on Create Free Account now writes Time Zone successfully. See decisions log. Mockup unchanged. Previous status, retained for history: Mockup (title shortened from "Create my Free Account" 2026-08-07; Phone Row-Tinted, §6.25). Time Zone field (2026-08-09) got a working demo-JS pull-down over every IANA zone name, defaulting to the browser-detected zone. **2026-08-10**: exact-match click shows the full Time Zone list with the current value highlighted (`.lookup-item.selected`) |
| Your Account — returning | `screens/WYP_your_account_palette1_floating.html` | `/account` | Mockup (Phone Row-Tinted 2026-08-07, §6.25) — **superseded in part, 2026-08-13**: `/account` is now live, but as an entirely new, deliberately minimal screen (`app/components/AccountForm.tsx`), not a conversion of this mockup. Owner: Private Category should be "an account option, not a standard presented data element... available for Free Accounts, but only if they turn it on." Offered a choice between building a sliver of Account now vs. adding the toggle to Housekeeping instead, the owner picked Account — ending its long run as "intentionally undesigned pending further product evolution." The live screen has exactly one control (a `.checkrow` toggle, "Show Private Category," auto-saving on change) and none of this mockup's other fields (Name/Email/Phone/Time Zone/tier/Change Email) — those remain exactly as undesigned as before. Migration 018 (`profiles.private_category_enabled boolean not null default false`) — confirmed run by the owner 2026-08-13. **Second toggle added 2026-08-13, same day**: "Show Due/Done Time (Requests)" (`profiles.request_time_enabled boolean not null default true`, migration 019, confirmed run by the owner 2026-08-13) — on by default, unlike Category, since this simplifies pre-existing behavior rather than introducing a new one; owner: "when turned off the four-value two-line presentation of Due Date Due Time Done Date Done Time on Requests would become like a ToDo one-line two-value presentation of Due Date and Done Date." Both toggles now share one generalized `handleToggle(field, next, setLocal)` helper. **Two more toggles added 2026-08-14**: "Show Due/Done Dates (ToDos)" (`profiles.todo_dates_enabled`, off by default, migration 022, DRAFTED not yet confirmed run — see Create ToDo/ToDo Detail's own entries below for the Status chip it gates) and "Subscribed? (testing only)" (`profiles.tier`, TESTING ONLY, migration 024, **confirmed run by the owner 2026-08-14** — reopens a self-serve write to `tier` that migration 002 deliberately withheld from `authenticated`; owner's own request frames this as a temporary stand-in for a real Subscription Details flow, to be replaced, not extended). The tier toggle writes via its own `handleTierToggle`, since `tier` is text-valued, not boolean. Same batch flipped `request_time_enabled`'s own default to `false` (migration 023, DRAFTED not yet confirmed run) — all three non-testing toggles are meant to be off/simple by default for a new account once that one is run too. This mockup itself is unchanged and still the reference for whenever the rest of Account gets built |
| Add Contact | `screens/WYP_add_contact_palette1_floating.html` | `/contacts/new` | Converted (Save wires to `contacts`; First/Last Name collapsed to a single Name field 2026-08-07, writes only `display_name` — migration 005, run; Phone Row-Tinted while `sendBy === 'email'`, §6.25). Time Zone (2026-08-09, migration 007 drafted) is a working required §6.16 lookup over every IANA zone name, defaulting to the owner's own `profiles.time_zone` else the browser's detected zone — see CLAUDE.md for the write-back-to-profiles reasoning. **2026-08-10**: Time Zone dropdown now visually highlights the currently-selected zone (`.lookup-item.selected`) — the `browsing` full-list-on-focus behavior was already correct from the 2026-08-09 fix, only the highlight was missing. This mockup itself has no `<script>` at all, so nothing to port here. **Origin-aware return path, 2026-08-11** — owner-reported: opening this screen from Create Request's own Add Contact button and saving landed on the Contacts list instead of back on the Request being created, with the new contact selected. This was a flagged, expected gap (see this file's own header comment, 2026-08-09) — Create Request's Add Contact button reached it before the not-yet-built §9.9.5 interception dialog did. Fixed by reading a `?from=create-request` query param (set by `CreateRequestForm.tsx`'s Add Contact link): Save now redirects to `/requests/new?newContactId=<id>` in that case instead of `/contacts` (the contact insert gained a `.select('id').single()` to get that id), and Cancel returns to `/requests/new` empty-handed rather than `/contacts`. Absent that param, both still behave exactly as before. Read via `window.location.search` inside the Save/Cancel handlers, not `useSearchParams()` — those handlers only ever run client-side already, so there's no SSR pass to guard against, and the hook would have forced a Suspense boundary onto `/requests/new`'s page for a value it never needs server-side |
| Request Response (was "Respond to Request" through 2026-08-07) | `screens/WYP_respond_to_request_palette1.html` | `/r/[token]` | **Live** (`app/components/RequestResponseForm.tsx`, 2026-08-10, Week 3 Days 2-3) — the one route in the app an anonymous, unauthenticated visitor reaches: the recipient of a Request, via the secure link. No `RequireAuth`. Reads and writes go only through the migration 008/009 `SECURITY DEFINER` functions (`get_request_by_token`, `set_response_done_by_token`, `add_dialog_by_token`) — never a raw table query, since `requests`/`dialog` deliberately have no `anon` policy. Category is never fetched or rendered (PRD §2.3 — caught as a bug in an early draft of `get_request_by_token` and fixed by migration 009 before it was ever run). No recipient name collection — Dialog entries added through this screen show `who` resolved server-side from the Request's own Contact. **Diverges from the mockup in two flagged ways**: (1) Done Date/Done Time render as Request Detail's real `.fgroup.frow`+`.ffloat.picker.native` editable pickers, not the mockup's boxed `.duo`/`.fieldval` static-text preview — and drop `.panel.req` entirely rather than resolve the mockup's own unresolved "should be conditional... not permanent" comment; both fields are optional here, using the ordinary `.opt` Row-Tint-while-empty treatment instead. (2) Add to Calendar is present but inert (`.ics` generation is out of scope this batch). Send calls `set_response_done_by_token` and shows an inline `.noticeband` confirmation rather than navigating anywhere (an anonymous visitor has no in-app destination to return to); Cancel resets the two editable fields to their last-saved values rather than `router.back()`, for the same reason. "Create your own Free Account" links to `/login`, the only live account-creation entry point. Dialog/Attachments panels reuse Request Detail's dynamic open-Question Answer-unlocking and locked-Attachments treatment verbatim. **Extended 2026-08-10, owner testing the live screen**: Attachments now only renders when the issuer is a subscriber (`owner_tier`, migration 011) — hidden entirely for a free-tier issuer rather than shown as generic locked/upsell noise; even when shown, Add Attachment stays inert since attachment storage doesn't exist anywhere in the app yet, so the copy reads "No attachments yet" rather than the (now inapplicable) subscription pitch. New **quick-Done band** (§6.31, `.donerow`/`.donenote`, PROPOSED) above the Done Date/Done Time row — a "Done" button that fills Done Date with today and gets out of the way; band text and button disabled-state react purely to whether Done Date holds a value, however it got there. **Neither of these two additions has been ported back into the mockup yet** — flagged, not silently skipped; the mockup's own Attachments block still shows a full demo file list (aspirational, beyond what's actually built) and its Done Date/Time are still static preview text, both already diverging from the live screen per the note above. **Word-wrap fix, 2026-08-10** — owner-reported on a narrow Android phone: pairing the Date/From/Due column with Add to Calendar beside it squeezed the column enough that the verbose weekday date format wrapped before the year. Add to Calendar moved to its own `.panelact` row above `.meta` (same pattern as Add Dialog/Add Attachment on this screen); `.metatop`/`.metacol` removed from `globals.css`. Date format itself is unchanged — the owner's own alternative proposal (reformat to mdy+weekday, keep the side-by-side layout) was set aside since the verbose format is shared with Request Detail/ToDo Detail/Response Detail/Dialog Detail's own date displays and reformatting it here alone would create inconsistency for a problem the layout change already solves. `handleQuickDone()` now scrolls Done Date into view on click, addressing the owner's own flagged concern that the layout change pushes it further down the screen. Ported to `WYP_respond_to_request_palette1.html` and, per the owner, to Response Detail's mockup as well (see that entry above). **Two more small fixes, same day**: Add to Calendar's row had no gap above it (nothing preceded it in `.scroll`) while keeping its usual 6px gap below — added a `.panelact-top` modifier, scoped to this one row rather than `.panelact` everywhere. The "Free Account Features" promo block dropped its kicker line entirely (redundant with the button's own "Free Account" label) and reordered to heading → button → sentence, so the descriptive sentence no longer reads as a precondition to clicking the button. Response Detail's mockup has no promo block to match (signed-in users don't see it), so only the Add to Calendar spacing fix — and note this one's scoped to Request Response, not ported to Response Detail — is Request-Response-specific here. **Add to Calendar is now real, 2026-08-10** — `handleAddToCalendar()`/`buildIcsContent()` generate an RFC 5545 `.ics` entirely client-side from data already on the page (no new migration or endpoint) and trigger a `Blob` download; no stored Due Time defaults to 9:00 AM (`ICS_DEFAULT_DUE_TIME`, flagged for a future per-account default), and the description's boilerplate wrapper text ("A Would You Please Request from `<name>`:" / "To mark it completed, click:") is hardcoded pending a future admin surface for editable standard text — both explicitly flagged as "will be done" items, not built. Ported to `WYP_respond_to_request_palette1.html`'s and `WYP_response_detail_palette1.html`'s own demo JS (`addToCalendar()`), using each mockup's own visible demo data. **`.ics` builder extracted to `app/src/lib/ics.ts`, 2026-08-11** — Response Detail (see that screen's own entry above) needed the identical logic; a rare exception to this codebase's usual per-component-duplication convention, since this is ~90 lines of stateless RFC 5545 logic with no component state involved, not a short one-liner like `todayISODate`/`formatMDY`. **Dialog and Attachments simplified at zero entries to the §6.32 `.actlabel`/`.actlabel.locked` row, same day** — live only; this mockup's Dialog thread is permanently seeded (empty state documented in a comment, not built) and its Attachments block is the separate, pre-existing aspirational populated-file-list demo already flagged above, left untouched except for an explanatory comment. **2026-08-11, later same day**: Done Date/Done Time now open their native picker on a click anywhere in the field, not just the calendar/clock icon (`openPicker`/`showPicker()`, owner-reported Windows behavior) — live only; this screen's mockup has no real `<input type="date"/"time">` fields, only static `.fieldval` text, so there's nothing there to open. **Two more fixes, same day**: the Done Date/Done Time row now splits evenly regardless of which field holds a value (`.frow .ffloat`'s flex-basis changed from `auto` to `0%`, app-wide — see §6.33's row); Done Time gained a §6.33 `.fclear` button to clear a set value, since Time fields had no way to do that (Date already did). **A third donerow state, same day**: once Send succeeds, the text next to the Done button changes to "This Request is now marked as Done and has been Sent." — reactive to the same `sendConfirmed` state that already drives the `.noticeband` above. Live only — the quick-Done band itself was never ported into this mockup (see the entries above), so there's no second/third state to add there. **Due/Done Time now driven by the issuer's own setting, 2026-08-13** (`owner_request_time_enabled`, migrations 019/020, confirmed run by the owner 2026-08-13) — rights on a Request come from its issuer, never the viewer, same precedent as the `owner_tier` Attachments gate above: the Due: `.metarow` drops its time suffix, and the editable Done Date/Done Time row collapses to Done Date alone, when the issuer has this turned off. Live only — this mockup has no owner_request_time_enabled to read. **Attachments are now real, 2026-08-14** (Week 5 Priority 3) — `owner_tier === 'subscriber'` still gates whether the segment renders at all (unchanged); when it does, `AttachmentsPanel` (`mode="file"`, `recipientToken` set, no `authToken`) replaces the old locked note. No delete UI here — an anonymous visitor has no session to attribute a delete to (`app/api/attachments/delete/route.ts` refuses without one regardless). Live only — this mockup's Attachments block is still the separate, pre-existing aspirational demo file list, unchanged |
| Create Request | `screens/WYP_create_request_palette1.html` | `/requests/new` | Converted (Send wires to `requests`; Recipient is a single Name lookup against `contacts.display_name` as of 2026-08-07 — was First/Last; Category lookup and Add Category are functional; Print is `window.print()`; Add Dialog opens a modal, §6.27 — Kind chips Question/Comment, Answer always locked since a Request starts with an empty thread; entries staged client-side and written to `dialog` — migration 004, run 2026-08-06; migration 006, confirmed run 2026-08-09 — together with the Request on Send; Due Time/Category carry Row Tint when empty, white once filled (2026-08-07); required fields (Recipient, Due Date, Description) carry an Ink border, §6.26; Add Dialog/Add Attachment sit above their full-width boxes rather than beside them (§6.26, 2026-08-07); title shortened from "Create a Request"; Attachments stays in the v1 locked state, permanently Row-Tinted; no in-place no-contact interception yet — see §6.24 below). **2026-08-10**: Recipient and Category lookups now show the full list (current value highlighted) on a click that would otherwise re-filter to one row — live component only, this mockup's own Recipient/Category fields have no interactive JS to update. **2026-08-11**: Dialog and Attachments simplified at zero entries to the §6.32 `.actlabel` row; this mockup has functional add/remove Dialog demo JS, so the toggle (`dlg-empty-row`/`dlg-fieldact`/`removeDialogRow()`) is real, not just documented. **Same day**: Due Date/Due Time now open their native picker on a click anywhere in the field, not just the calendar/clock icon (`openPicker`/`showPicker()`, owner-reported Windows behavior) — live component only, this mockup's own date/time fields are static `type="text"` display, not real pickers. **Two more fixes, same day**: the Due Date/Due Time row now splits evenly regardless of which field holds a value (`.frow .ffloat`'s flex-basis changed from `auto` to `0%`, app-wide — see §6.33's row below for the full reasoning); Due Time gained a §6.33 `.fclear` button to clear a set value, since Time fields had no way to do that (Date already did). **A third fix, same day**: Add Contact now links to `/contacts/new?from=create-request` instead of a bare `/contacts/new`, so `AddContactForm.tsx` (see that screen's own entry below) knows to send the owner back here — with the new contact selected on Save — instead of always landing on the Contacts list; the rest of this screen's fields are still lost on that round trip, unchanged, pending the real §6.24/§9.9.5 in-place interception dialog. Live component only — this mockup's Add Contact button has no interactive JS of its own to update, same as Recipient/Category. **Category is now conditional, 2026-08-13** (migration 018, `profiles.private_category_enabled`, off by default) — the entire Category `.fgroup` (lookup + Add Category) is unrendered when off; unaffected when on. **Due Time is now conditional too, same day** (migration 019, `profiles.request_time_enabled`, on by default, confirmed run by the owner 2026-08-13) — the Due row's Due Time field is simply unrendered when off, leaving Due Date alone (no Done Date/Time fields exist here at all — those only appear once a Request has been sent). Mockup unchanged — always draws both fields as present. **Attachments are now real, 2026-08-14** (Week 5 Priority 3) — the locked `.donerow` state is replaced with a subscriber-gated staged-file panel (own `tier`, fetched alongside the other profile flags): files are held as real `File` objects in memory, same pattern as staged Dialog entries, and uploaded via `/api/attachments/upload` only after Send has a real request id. Migrations 025/026 confirmed run by the owner 2026-08-14. Mockup unchanged |
| Add Contact — no contact dialog | `screens/WYP_add_contact_no_contact_dialog_palette1.html` | `/contacts/new` | Mockup. Time Zone field (2026-08-09) got the same demo-JS pull-down as Create Free Account, for consistency — this is a duplicate of Add Contact's own Time Zone field, kept in sync rather than left to drift now that Add Contact's live version works. **2026-08-10**: exact-match click now shows the full Time Zone list with the current value highlighted (`.lookup-item.selected`) — see decisions log |
| Create ToDo | `screens/WYP_create_todo_palette1.html` | `/todos/new` | **Live** (`app/components/CreateTodoForm.tsx`, 2026-08-09). Save wires to `requests` with `contact_id` null; Priority chip row (ASAP/SOON/LATER); Category lookup and Add Category functional, same pattern as Create Request; Add Dialog opens a modal, §6.27, Question/Comment only (Answer always locked — an empty thread). §9.4 had no mockup at all before 2026-08-06 — see decisions log; title shortened from "Create a ToDo", stacked Dialog/Attachments action rows and Ink-bordered required Description added 2026-08-07. **Due Date added 2026-08-10** — closing out Week 3's last open item — optional (`.opt`, not Create Request's required `.req` border), written as `null` when empty; `requests.due_date` was already a plain nullable column, no schema change or sentinel-value workaround needed. **Done Date added the same day**, combined into one row with Due Date per the owner's own rough draft — "the reason a Create ToDo should allow a Done Date is to allow completed ToDos to be entered if desired." No Done Time on either field — ToDos don't use it, unlike a Request's Done Date/Time pair. **2026-08-10**: Category lookup now shows the full list (current value highlighted) on a click that would otherwise re-filter to one row — live component only, this mockup's Category field has no interactive JS. **Same day**: quick-Done band (§6.31, `.donerow`/`.donenote`, mirroring Request Response's) added above the Due Date/Done Date row — a "Done" button that fills Done Date with today; wired with real demo JS (`quickDone()`) in this mockup, unlike Request Response's own mockup, which still hasn't gotten the feature ported (see that screen's entry above). **2026-08-11**: Dialog and Attachments simplified at zero entries to the §6.32 `.actlabel` row, with real functional toggle JS (same pattern as Create Request) since this mockup's Dialog demo genuinely goes from empty to populated. **Same day**: Due Date/Done Date now open their native picker on a click anywhere in the field, not just the calendar/clock icon (`openPicker`/`showPicker()`, owner-reported Windows behavior) — live component only, this mockup's date fields are static `type="text"`, not real pickers. **One more fix, same day**: the row now splits evenly regardless of which field holds a value (`.frow .ffloat`'s flex-basis changed from `auto` to `0%`, app-wide — see §6.33's row); no `.fclear` needed here, ToDos have no Time fields. **Category is now conditional, 2026-08-13** (migration 018, `profiles.private_category_enabled`, off by default) — the entire Category `.fgroup` is unrendered when off; unaffected when on. **Due/Done Dates now conditional too, 2026-08-14** (migration 022, `profiles.todo_dates_enabled`, off by default, DRAFTED not yet confirmed run) — off collapses the quick-Done band and Due Date/Done Date row into a single §6.35 PROPOSED Status chip row (Open/Done, reusing `.sendrow`+`.chippair`+`.gatenote` verbatim from Add Contact's Send Requests By picker), matching the owner's own pasted ToDo Detail mockup. A new ToDo always starts Status at Open. **Locations are now real, same day** (Week 5 Priority 3) — the locked `.donerow` is replaced by a staged Description+Location mini-form (own `tier`, subscriber-gated same as a Request's Attachments — a design call made without an explicit owner instruction, flagged in the decisions log), inserted directly as `kind = 'reference'` rows (migration 025's RLS allows this without an API route) once Save has a real id. **Locations UX refined, same day** — a persistent `.donerow`/`.donenote` note ("Locations can be used for URLs or File paths.") now sits next to Add Location whether or not any are staged yet, from the owner's own reference screenshot; a saved http(s) URL renders as a real underlined link (`.attname a`, added to `globals.css` — Tailwind's preflight had been stripping the default anchor underline); a non-URL entry (a file path) gets a "Copy" button instead, since a browser has no filesystem access to open one the way a real Attachment opens — see the decisions log for why a `file://` link was rejected. **URL detection widened, same day** — `urlLocationHref()` now recognizes a bare domain (`ft.com`, `www.ft.com`), not just a full `https://` URL, while still excluding file-path shapes and common file extensions (`report.pdf` stays plain text); a live HEAD/DNS-style reachability check was considered and rejected (CORS, SSRF, and it answers the wrong question) — see the decisions log. Note text corrected to "Locations are URLs or File paths." Mockup unchanged |
| Storage Maintenance | `screens/WYP_storage_maintenance_palette1.html` | `/storage` | Mockup (Close corrected from `.btn-secondary` to `.btn` 2026-08-07 — it's the only button on the band, so Secondary's "lower emphasis than a Primary sibling" meaning didn't apply) |
| Storage warning strip (§6.18) | `screens/WYP_storage_warning_strip_palette1.html` | — | Component study |
| Request Detail | `screens/WYP_request_detail_palette1.html` | `/requests/[id]` | **Live** (`app/components/RequestDetailForm.tsx`, 2026-08-09, extended 2026-08-10). Issuer's own view of a Request already sent — built from Create Request's mockup. Recipient shown as non-modifiable label:value text (`.metarow`, not a boxed field — §6.28); Due Date/Time, Done Date/Time, Category, Description all fetch pre-filled and update on Send. Notice band (Strip background) under the title: "Note: The Request Recipient is notified of changes." Dialog panel reads the real `dialog` thread (not a staged list) and each Add Dialog Save writes immediately, since the Request already exists; Add Dialog's Kind picker is dynamic (open-Question rule) — Answer unlocks only when a Question in the thread is still open, with a which-Question picker above one open Question. Selects `dialog.replies_to_id` (migration 006, confirmed run by the owner 2026-08-09). Action buttons stay Send/Cancel, unchanged. Both return via `router.back()`, not `push('/')` — see the Main Screen entry above. **Response Link band** (§6.30, PROPOSED, 2026-08-10, Week 3 Day 4) sits directly under the notice band: a "Get Response Link" button calling `issue_request_link` (migration 008), then the resulting `/r/[token]` URL with Copy/Regenerate — the copy-link stand-in for real email delivery this week. Depends on migrations 008/009, both still DRAFTED as of this writing. **2026-08-10**: Category lookup now shows the full list (current value highlighted) on a click that would otherwise re-filter to one row — live component only, this mockup's Category field has no interactive JS. **2026-08-11**: Attachments simplified to the §6.32 `.actlabel.locked` row (live and mockup); Dialog simplified live only — this mockup's thread is permanently seeded with demo data, so its empty state is documented in a comment rather than built as unreachable toggle JS. **Same day**: Due Date/Due Time/Done Date/Done Time now open their native picker on a click anywhere in the field, not just the calendar/clock icon (`openPicker`/`showPicker()`, owner-reported Windows behavior) — live component only; this mockup's own date/time fields are static `type="text"` display, not real pickers, so there's nothing to open. **Two more fixes, same day**: this screen was the specific one that surfaced the Due Date/Due Time and Done Date/Done Time row-width bug (Due Date rendering wider than Due Time and squeezing its label off the edge, since this screen's fields load pre-filled unevenly, unlike Create Request's always-empty ones) — fixed app-wide via `.frow .ffloat`'s flex-basis (§6.33's row below); and Due Time/Done Time both gained the §6.33 `.fclear` button. **Due/Done Time is now conditional, 2026-08-13** (migration 019, `profiles.request_time_enabled`, confirmed run by the owner 2026-08-13) — the two two-value rows (Due Date/Due Time, Done Date/Done Time) collapse into one combined row, Due Date + Done Date, reusing ToDo Detail's own combined-row markup rather than inventing a new layout. **Get Response Link removed, 2026-08-14** — owner: "no longer needed for testing." The `.linkband` band (§6.30 PROPOSED) and its state/handlers are gone from the live component; the underlying `issue_request_link` function is untouched, still used by Create Request's own automatic email flow. Mockup unchanged — it never had this band drawn in the first place. **Attachments are now real, same day** (Week 5 Priority 3) — the locked `.donerow` is replaced by `AttachmentsPanel` (`mode="file"`), gated on the signed-in owner's own `tier`; upload/list/delete go through `app/api/attachments/{upload,list,delete}` (service_role, server-only — see migration 025/026's own header comments in `docs/Week5 - SQL history.txt`). Migrations 025/026/027 confirmed run by the owner 2026-08-14. Mockup unchanged |
| Response Detail | `screens/WYP_response_detail_palette1.html` | `/requests/[id]/respond` | **Live** (`app/components/ResponseDetailForm.tsx`, 2026-08-11, migration 012) — superseding the "Mockup" status below. Reached by clicking a row in Main Screen's now-live Received section. All data through the four new `SECURITY DEFINER` functions keyed on the caller's own session email against the sending Contact's email (`get_received_request`, `set_response_done_as_recipient`, `add_dialog_as_recipient`, plus the list-shape `get_received_requests` Main Screen itself calls) — same reasoning as the anonymous `/r/[token]` functions, extended to the signed-in case: RLS can't hide Category from an otherwise-visible row, so a function allow-lists what a recipient may touch instead of a policy. Wrapped in `RequireAuth`, unlike `/r/[token]`. A close adaptation of `RequestResponseForm.tsx`: same quick-Done band and `owner_tier`-gated Attachments segment (both diverge from this file's own still-mockup-era markup below — not yet ported into the static HTML), minus the "Create your own Free Account" `.promo` block (this visitor already has an account), and Cancel uses `router.back()` instead of a local-state reset (this screen, unlike an anonymous mailed link, is always reached from a real Main Screen history entry — same convention as Request Detail/ToDo Detail/Contact Detail). The `.ics` builder is shared with Request Response via `app/src/lib/ics.ts`, extracted the same day rather than duplicated a second time. Mockup file itself is now stale relative to this — flagged, not silently diverged from: originally, new 2026-08-08, built from Request Response's mockup for a signed-in user viewing/continuing a Received Request from inside the app, as opposed to the anonymous secure `/r/[token]` link — same fields and dynamic Add Dialog logic, minus the "Create your own Free Account" `.promo` block, which doesn't apply to someone who already has an account. Shares its demo Dialog thread with Request Detail so the same Request reads consistently from both sides. **2026-08-10**: Add to Calendar moved to its own row above Date/From/Due (`.panelact`, `.metatop`/`.metacol` removed), matching the word-wrap fix ported to Request Response — see that entry below. **Still not ported into this mockup**: the quick-Done band and `owner_tier`-gated Attachments segment the live screen now has. **2026-08-11**: Dialog and Attachments simplified at zero entries to the §6.32 `.actlabel`/`.actlabel.locked` row — live only, same reasoning as Request Response above (seeded thread, and the separate pre-existing aspirational Attachments demo left untouched). **Same day**: Done Date/Done Time now open their native picker on a click anywhere in the field (`openPicker`/`showPicker()`) — live only; this screen has no real `<input type="date"/"time">` fields in its mockup at all, only static `.fieldval` text, so there's nothing there to open. **Two more fixes, same day**: Done Date/Done Time now split evenly regardless of which field holds a value (`.frow .ffloat`'s flex-basis changed from `auto` to `0%`, app-wide — see §6.33's row); Done Time gained a §6.33 `.fclear` button — both live only, same reasoning as above. **A third donerow state, same day**: once Send succeeds, the text next to the Done button changes to "This Request is now marked as Done and has been Sent." — reactive to the same `sendConfirmed` state driving the `.noticeband` above, live only, same reasoning as Request Response. **Due/Done Time now driven by the issuer's own setting, 2026-08-13** — same `owner_request_time_enabled` gate as Request Response above (migrations 019/020, confirmed run by the owner 2026-08-13), live only. **Attachments are now real, 2026-08-14** (Week 5 Priority 3) — same `owner_tier` gate and `AttachmentsPanel` (`mode="file"`) as Request Response, but with a real session: this signed-in recipient can delete their own uploads (never the owner's), unlike the anonymous screen. Live only |
| ToDo Detail | `screens/WYP_todo_detail_palette1.html` | `/todos/[id]` | **Live** (`app/components/TodoDetailForm.tsx`, 2026-08-09). Started as an exact duplicate of Create ToDo except the title (owner's original instruction); two deviations added going live, both flagged rather than silently applied: a Done Date/Time row (owner-confirmed via AskUserQuestion — without it there was no way to mark a ToDo Done through the UI at all) and a Dialog panel that reads the real thread with dynamic Answer unlocking, same as Request Detail, rather than Create ToDo's blank staged list — an existing ToDo can already have Dialog entries. Same `dialog.replies_to_id` dependency as Request Detail — migration 006, confirmed run by the owner 2026-08-09. **Due Date added 2026-08-10** alongside Create ToDo's own new field, beyond the owner's literal ask — a Due Date set at creation needs somewhere to stay visible and editable afterward, same as every other field here; same `.opt` optional treatment as Create ToDo. **Combined with Done Date into one row, and Done Time removed, the same day** — matching the owner's own pasted rough draft and his "the ToDos do not need Done Time" instruction; `requests.done_time` stays in the schema (shared with Requests) but ToDo screens no longer read, write, or display it. **2026-08-10**: Category lookup now shows the full list (current value highlighted) on a click that would otherwise re-filter to one row — live component only, this mockup's Category field has no interactive JS. **2026-08-11**: Attachments simplified to the §6.32 `.actlabel.locked` row (live and mockup); Dialog simplified live only — this mockup's thread is permanently seeded, so its empty state is documented in a comment rather than built as unreachable toggle JS. **Two more fixes, same day, both owner-reported testing the live screen**: (1) the quick-Done band (§6.31, `.donerow`/`.donenote`) was missing entirely — Create ToDo gained it 2026-08-10 but that batch never ported it here, unlike Response Detail's parallel case; added, verbatim wording, same reactive-to-Done-Date behavior; not yet ported into this mockup (same as Response Detail's own unported band). (2) Due Date/Done Date now open their native picker on a click anywhere in the field, not just the icon (`openPicker`/`showPicker()`, owner-reported Windows behavior) — live component only, this mockup's date fields are static `type="text"`, not real pickers. **A third fix, same day, and the one that actually surfaced this whole batch**: this screen's Due Date rendered wider than Done Date and squeezed the second field's label text off the edge on a phone (Create ToDo's identical row scaled evenly, since its fields start empty; this screen loads an existing, often pre-filled record) — fixed app-wide via `.frow .ffloat`'s flex-basis (§6.33's row); no `.fclear` needed, ToDos have no Time fields. **Category is now conditional, 2026-08-13** (migration 018, `profiles.private_category_enabled`, off by default) — the entire Category `.fgroup` is unrendered when off; unaffected when on. **Due/Done Dates now conditional too, 2026-08-14** (migration 022, DRAFTED not yet confirmed run) — same collapse as Create ToDo's own entry above, into a single Open/Done §6.35 Status chip row. Unlike Create ToDo, this screen's initial Status is derived once from whatever `done_date` the record already has, and saving with Status left on "Done" preserves that original value rather than overwriting it to today. **Locations are now real, same day** (Week 5 Priority 3) — `AttachmentsPanel` (`mode="reference"`), same tier gate and RLS-direct insert/delete as Create ToDo above; no API route involved, unlike a Request's real file Attachments. **Locations UX refined, same day** — same `.donerow` note, clickable-URL, and Copy-path-for-file-paths additions as Create ToDo above, both live in the shared `AttachmentsPanel.tsx`, including the same-day bare-domain URL detection widening and corrected note wording. Mockup unchanged |
| Dialog Detail | `screens/WYP_dialog_detail_palette1.html` | — (proposed, opened from a Dialog list entry) | Mockup, new 2026-08-08, **rewritten same day**. **Read-only, on purpose** — see the file's own header comment: `dialog` has no UPDATE/DELETE policy (migration 004, append-only), so a past entry can be viewed here, never edited. Originally built to mirror the Add Dialog modal (a chip row for Kind); replaced hours later with the §6.28 label:value pattern once the owner set the app-wide non-modifiable-fields rule — Dialog Entry Type is now a bold label-as-value ("Answer:") over a horizontal rule (`.dlgtype`/`.dlghr`) rather than a chip row, since one entry has exactly one Kind and there was nothing left for a chip row to distinguish. Date/From use `.metarow`. Single Close button (Primary — same "only button on the band" rule as Storage Maintenance) |
| Contact Detail | `screens/WYP_contact_detail_palette1.html` | `/contacts/[id]` | **Live** (`app/components/ContactDetailForm.tsx`, 2026-08-09), new. "Exactly the same as Create Contact except for the screen title" (owner's instruction), with one deliberate difference: **Save + Close**, not Save + Cancel — Close is what every other exit-an-existing-record screen in this app uses (Dialog Detail, Storage Maintenance); Cancel specifically means "discard what I was about to create," which doesn't apply here. Mirrors AddContactForm.tsx's actual live fields, now including Time Zone (2026-08-09, migration 007 drafted) — the same required §6.16 lookup as Add Contact, prioritizing this contact's own stored zone over the owner's-profile/browser-detection fallback chain used when one isn't set yet. Save and Close both return via `router.back()`, restoring the Contacts list's scroll position instead of landing at its top. **2026-08-10**: this mockup's own Time Zone field was already flagged as not wired (see the file's own header comment) — no change needed; the live component's Time Zone dropdown now visually highlights the currently-selected zone (`.lookup-item.selected`), same as AddContactForm.tsx |
| Contacts (titled "My Contacts" for a few hours on 2026-08-09) | `screens/WYP_contacts_list_palette1.html` | `/contacts` | **Live** (`app/components/ContactsList.tsx`, 2026-08-09), new. Owner's spec, verbatim: "should show the name, notify method and related value (email or phone) - and upon click open up a Contact Details screen for editing (and should have a Close button)." Reuses `.hkrow`/`.hktitle`/`.hknote` (§6.23) rather than inventing a new list style — structurally the same "navigational row" as Housekeeping's own rows. Sorted alphabetically by `display_name`. Reached from the Main Screen's Housekeeping "Contacts" row. **Retitled from "My Contacts" the same day**: adding the requested Close button alongside Add Contact would wrap the "My Contacts" title on Android, and separately the owner's rule is that a nav row's label must repeat its destination's title exactly — so the Housekeeping row became "Contacts" too, not "My Contacts". Band now `.bandcluster`-wrapped (Add Contact + Close, `.btn`/`.btn-secondary`) instead of a single right-margined `.btn`. Close and a successful Add Contact save both return here via `router.back()`, not a fresh `push`, so the list's own scroll position is preserved (see Main Screen's entry below) |
| Archive | `screens/WYP_archive_palette1.html` | `/archive` | **Live** (`app/components/ArchiveForm.tsx`, 2026-08-14) — converted the same day as the mockup fixes below. Per-viewer archive scope (owner-confirmed via AskUserQuestion): migration 028 (confirmed run by the owner 2026-08-14) adds two independent nullable columns to `requests` — `archived_at` (the row's own owner archiving it from Sent or a ToDo's only list, plain-RLS-writable) and `received_archived_at` (the recipient archiving their own Received copy, via a new `archive_received_request()` SECURITY DEFINER function, same email-match-through-contacts pattern as `set_response_done_as_recipient`/`add_dialog_as_recipient`). Neither column is ever excluded from a query — matches the owner's own drafted §9.5 replacement text ("no longer displayed... while remaining available through Search"): `MainScreen.tsx` fetches Sent/Received/ToDos in full and hides an archived row only while its search box is empty, re-including it the moment a query is typed. `ArchiveForm.tsx` itself only shows a Done record that is NOT yet archived (Un-Archive stays deferred). Before Done Date uses the app's real `.ffloat.picker.native` component here (the mockup's `.plaingroup` substitution was only needed for the standalone file's missing Tailwind reset) plus the new calendar-picker-only `keydown` block — the one date field in the app with that behavior, not retrofitted onto the other 14. Reached from Main Screen's Housekeeping "Archive" row (`MainScreen.tsx`, new). **Three live-testing fixes, same day**: Recipient and Before Done Date now wrap in `.fgroup > .frow > .ffloat` (matching every other field of this shape in the app) instead of a bare `.fgroup > .ffloat`, which had left `.ffloat` an unstretched inline box and thrown off the floating label's position; the Record Type chip row's selected state now has its own `.archtyperow .chip.sel` rule (the `.sel` toggle it ported from the mockup was scoped to `.chips` elsewhere in the app, which this row doesn't use); and Record Type/Recipient-Requestor query/Before Done Date/per-type deselected sets are now persisted to `sessionStorage`, since a row click navigates to the record's Detail screen and back via `router.back()`, which fully remounts this component. **Column-header sorting added, 2026-08-14** (owner-reported: "the sorting does not work for column headings... it would be helpful to have it react to users as they have learned to expect") — the colbar's To/From, Date, Due, Done (Sent/Received) and Priority (ToDos) headers, ported from the mockup as static decoration, are now real sort buttons duplicating Main Screen's own `ColSort` pattern (2026-08-11); each Record Type's sort persists to `sessionStorage` independently. Required extending the shared `Row` type with raw `dueISO`/`dateISO`/`priority` fields alongside the already-formatted display strings, since MM-DD-YY doesn't sort correctly as a plain string; `name` also changed from a `'—'`-baked-in fallback to a real nullable value so missing names sort to the end regardless of direction. **Recipient/Requestor query and Before Done Date now persist across Record Type chip switches, same day** (owner: entries "remain until changed within a session... regardless of which chip is selected") — `selectType()` no longer clears either on a chip click. **PRD §9.5's replacement text is still only drafted in the decisions log, not yet merged into the docx** — the live behavior above already matches that draft; the `.docx` itself is a separate, deferred step (see CLAUDE.md's Known gaps). See the decisions log's 2026-08-14 entry for the full write-up. Mockup history below, superseded for behavior (not for visual reference) by the live component:

**Mockup, 2026-08-14** — owner's own drafted UI strategy, pasted-in reference screenshots for the Sent Requests and ToDos states (Received Requests described as "very similar to the Requests Sent mockup," not separately screenshotted). One file, three states switched by a Record Type chip row (Sent Requests / Received Requests / ToDos, default Sent Requests) — everything below the chip row (explanatory text, filter fields, list) re-renders per the owner's own instruction ("everything below the banner is modified based on the chip selection"). Built with real, working demo JS rather than static markup, since the one thing worth actually proving out is the owner's own selection-persistence rule (see decisions log) — not just showing what the screen looks like. **Starts empty**: no records show until at least one filter (Recipient/Requestor and/or Before Done Date) is entered, matching the owner's own description ("would open with no Sent Requests displayed"); ToDos has no Recipient field at all (a ToDo has no recipient), so only Before Done Date gates it. A matching record's checkbox defaults checked; unchecking adds it to a per-Record-Type deselected set that a filter change never clears — the exact rule the owner walked through by example (deselect two rows under a Recipient filter, then add a Before Done Date, and the two stay deselected if they still qualify). Deselection and archived-status are independent *per Record Type* — switching chips is a different list, not a filter refinement of the same one. "Archive Selected" removes the currently-checked matches from that Record Type's own list (demo-only, nothing persisted past a reload) and shows a brief confirmation strip; the button is disabled at zero selected and otherwise shows a running count. Recipient/Requestor is a real, working type-ahead (distinct names in the current Record Type's own data, filtered as typed) — a simplified version of the app's §6.24 lookup, skipping the exact-match-full-list-on-click refinement other lookups have, to keep scope contained. **One correction from the reference screenshots, flagged rather than silently reproduced**: one row in the owner's own Sent Requests screenshot rendered in the app's red "Overdue" treatment — but every row on this screen is already Done (only Done records ever qualify for Archive), and Overdue/Done are mutually exclusive under `statusFor()`'s own rule, so a record here can never actually be Overdue. Built every row in the existing grey "Done" treatment instead (`.row.done`), consistent with how Main Screen already renders a Done-filtered row; almost certainly a copy-paste artifact in the owner's own reference (both screenshots reuse Main Screen's demo names — Jim G. Gillon, Maximillan Snyder, Beatrice Barrington — so the row's markup likely carried its class over unchanged from wherever it was copied). **Before Done Date uses a real `type="date"` input** (`.ffloat.picker.native`), not the `type="text"` static display every other mockup in this repo uses for a date field — a deliberate exception, needed so the demo JS has an actual comparable value to filter the seeded records by, and consistent with what a live conversion would use anyway (Request Detail, ToDo Detail, etc. already do). New `.archrow`/`.archcheck`/`.archspacer`/`.archbody` (§6.36 PROPOSED, see the table below). **Housekeeping entry point added to the Main Screen mockup only** (`WYP_main_screen_palette1.html`, new "Archive" row, label repeats the destination title per the existing naming rule) — **not** ported into `MainScreen.tsx`, since there's no live `/archive` route yet to point it at. **Not converted to live** — this pass is the design step only; see CLAUDE.md's Known gaps and the decisions log for the PRD §9.5 (v12.9) divergences this design surfaces, flagged for the owner rather than silently overwritten. **Four rendering fixes, same day, from the owner's own screenshots**: Before Done Date no longer uses the live app's `.ffloat.picker.native` label-over-input pattern (it depends on a Tailwind reset this standalone file doesn't have, and the native placeholder wasn't respecting the padding split without it) — now a plain static label above a normally-padded input (`.plaingroup`/`.finput.plain`); Archive Selected dropped its disabled/grey state and stays primary blue always; the Record Type chip row gained the Strip background every other chip row in the app sits on; and each row's body is now clickable (`.archbody`, hover/focus styling, keyboard-accessible) to represent opening the record's own Detail screen once this goes live — inert in this standalone file, but shows an inline note naming the target screen rather than doing nothing. **Four more fixes, same day, second round**: Recipient/Requestor's floating label overlapping typed text, fixed with an explicit `line-height: 1.2` on `.finput`/`.ffloat .flabel` (best-diagnosis, not screenshot-confirmed — no headless browser reachable in this sandbox); the date-box-looks-different-from-elsewhere question answered directly (deliberate, already-flagged divergence — the live app's `.ffloat.picker.native` needs a Tailwind reset this standalone file doesn't have); the calendar icon no longer reads as separately clickable — the whole `.plaingroup` label+input region is now one uniform `cursor:pointer` unit with a `focus`→`showPicker()` handler; and Before Done Date is now calendar-picker-only, a `keydown` listener blocking every key but Tab so manual m/d/y entry (uncontrolled rollover, no Feb 29 check, arbitrary years) is no longer possible at all — chosen over `readonly`, which risks inconsistently blocking the native picker-open interaction itself across browsers. See the decisions log for the full write-up |

`WYP_component_states_palette1.html` (this folder, not `screens/`) is the
reference for every control state. It is not a screen.

## Marketing pages

| Page | Mockup | Route | Status |
|---|---|---|---|
| Landing page (wouldyouplease.com, no parameters) | `marketing/WYP_landing_page.html` | `/` for an unauthenticated, no-parameter visit | **Live** (`app/components/LandingPage.tsx`, 2026-08-13) — superseding the "Draft... not yet wired to a route" status below. Owner: "I want to have people come to the website and see the WYP_landing_page.html... [not] be directed to /login." `app/page.tsx` no longer wraps `/` in `RequireAuth` (which unconditionally `router.replace('/login')`s an anonymous visitor); it now checks `supabase.auth.getUser()` itself and renders `MainScreen` when a session exists, `LandingPage` otherwise — every other authenticated route keeps using `RequireAuth` unchanged. A returning, already-signed-in visitor on the same device is unaffected: "Keep Me Signed In" persists the session via `localStorage` (`supabaseClient.ts`'s existing `REMEMBER_KEY`/`hybridStorage`), so they still land straight on `MainScreen`, same as before this change. Content and the hero SVG are carried over verbatim from the mockup — only mechanical JSX conversions were made (`class`→`className`, kebab-case SVG attributes→camelCase, `<a>`→`next/link`'s `<Link>` for in-app destinations, no re-loaded Google Fonts `<link>` since `next/font`'s self-hosted Inter is already in scope app-wide). Styles live in a new `app/components/landing.css`, imported only by `LandingPage.tsx` — every selector scoped under a `.wyp-landing` root class to avoid leaking into `app/globals.css`'s own site-wide rules; the one real collision found (`.panel`, already used by the app's own Dialog/Attachments panel) is renamed `.lpanel` here rather than relying on scoping specificity alone. `:root` tokens are not redeclared — `landing.css` reads the same custom properties `globals.css` already defines. Previous status, retained for history: Draft, 2026-08-12, not yet reviewed or wired to a route. Owner ask: a sales pitch whose one action item is the Free Account signup. Copy adapted from the owner's own sales one-pager (`WYP onepager.html`, uploaded) rather than written fresh. Hero illustration is a hand-built SVG (a simplified, legible version of the app's own Main Screen with two floating callout badges) rather than the owner's AI-generated Facebook-cover placeholder image he supplied as visual reference — that image's own "dashboard" content is garbled nonsense text and there's no image-generation tool available in this session to produce a replacement photo; the owner also said directly he didn't like that image's gold/yellow lettering, though he did like its right-side device-plus-badges composition, which this SVG keeps. **Mobile-first** (owner, 2026-08-12: "the primary people going to this landing page would be mobile phone users") — single-column stacking at rest, widened via two `@media` breakpoints (600px, 900px) rather than a desktop-first design narrowed down; the hero illustration sits below the headline/CTA on a phone rather than beside it, so the sales copy and button are the first thing seen. Both "Start Free Account" and "Sign In" point to `/login` — the same dual-purpose entry point every other sales-facing screen in this app already uses (see "Sign-up flow" below, which already anticipated a "sales page" leading here). Self-contained, single file, no dependency on `tokens.css`/`components.css`, matching every other mockup in this folder — but pulls its own `:root` values from the canonical token set (`design/screens/tokens.css`) rather than the one-pager's own hardcoded hex, so it can't drift from the live app's palette. Not part of the app's palette-1 component system (`.finput`, `.band`, `.frame-none`, etc.) — a different category of page, full-width and responsive rather than a 480px app frame; see `marketing/` in the Folders table above |

## Sign-up flow

There is no sign-up screen, because `signInWithOtp` creates the account on first
use and cannot tell whether the address already exists. Both entry points make
the identical call; they differ only in wording, and the paths diverge *after*
the link is clicked, based on whether the user has a profile row yet.

```
sales page ──→ "Start my Free Account" ─┐
                                        ├─→ email sent ─→ [link] ─→ /auth/callback
returning  ──→ "Sign In" ───────────────┘                              │
                                                  profile row? ──no──→ Create my Free Account ──→ main
                                                        │
                                                       yes ─────────────────────────────────────→ main
```

Band label, form caption, and the confirmation line all change with the mode —
three strings, one screen. Only the join path mentions Create my Free Account,
because only the join path leads there.

Recipients reach the same funnel from the "Create your own Free Account" block
on Request Response (renamed from "Respond to Request" 2026-08-08, for
consistency with Request Detail / Response Detail / Dialog Detail — see the
decisions log; same file, `screens/WYP_respond_to_request_palette1.html`).

Two rules fall out of this, both settled 2026-08-02:

- **First run shows Save only.** No Cancel, no Sign out. The user is
  authenticated but has no profile row, so an exit at that moment leaves an auth
  user who owns nothing and lands back on the same screen next time.
- **First run suppresses the subscription banner and ad slot.** An upsell and an
  ad before the person has seen the product is a poor first impression. Both
  appear on every screen after this one.

Status values: **Mockup** (design only) → **Converted** (React component exists)
→ **Live** (deployed and wired to real data).

## Conventions

**Naming:** `WYP_<screen>_palette<N>[_variant].html`

**One canonical file per screen.** Git holds the history — do not keep
`_old` / `_retired_<date>` copies alongside the current one. A retired `.tsx`
copy with a paste error inside `app/` broke the build on 2026-08-02; that is
the failure mode this rule exists to prevent.

**Self-contained files.** Images are embedded as data URIs and the only
external reference is the Inter webfont, so a mockup can be opened directly
from disk with no build step and no missing assets.

**Tokens are copied, not invented.** The `:root` block in every mockup must
match `app/globals.css`. Two reconciliations, both settled 2026-08-03: the
focused-field fill is `--focus-tint` everywhere (it was `--field` in four older
mockups and in `globals.css`), and the locked-control treatment is `.is-locked`
everywhere (Create Request briefly used a solid `--ink-soft` `.btn-locked`,
which read as an active button). When a mockup needs a component the system does not
have, add it with a `PROPOSED` comment and a `§` number so it can be folded
into the spec deliberately.

## Proposed components awaiting spec entry

§6 of the spec is occupied through **§6.18**. Anything new starts at §6.19 —
check the spec's table of contents before assigning a number.

| § | Class | Purpose | First used |
|---|---|---|---|
| §6.19 | `.btn-block` | Full-width primary action for auth screens, which have no band cluster | Sign In |
| §6.20 | `.checkrow` | Single persistent toggle; chips only cover either/or choices | Sign In |
| §6.21 | `.btn-quiet` | Global action that must stay reachable without competing with Create | Main screen — Log Out |
| §6.22 | `.is-locked` + `--locked-ink` / `--locked-border` | Action unavailable because of the request's tier | Request Response — Add Attachment |
| §6.10 variant, SUPERSEDED 2026-08-08 | `.finput[readonly]` + `--locked` | Read-only field, boxed like an editable input with a dashed border. Retired the same day it would have spread to a third screen — see §6.28 | was: Your Account, Create Free Account, Request Detail (all converted) |
| §6.23 | `.hkrows`/`.hkrow`/`.hktitle`/`.hknote` | Navigational row — bold Brand-Blue title, em-dash, regular Ink description, 2-line clamp (same visual language as the §6.5 ToDo row, distinct class since the meaning isn't Priority). No chevron: tappable by the same row-context convention as every other list row in the app. First entry point for Contacts and Account Profile, which otherwise have none — see decisions log 2026-08-05 | Main screen — Housekeeping |
| §6.24 | `.lookup-results`/`.lookup-item`/`.lookup-empty` | Pull-down list under a §6.16 lookup field. Neither the mockup nor the spec draws this state — only the resting field is shown, and the mockup's own comment ("selecting from either field's pull-down fills both fields") implies a list without designing one. Kept plain: Row Tint hover, Rule dividers, no new visual language. Needs a real design pass if it earns a dedicated screen state later. Behavior rule added 2026-08-07: on focus, browse the full list immediately if it has fewer than 12 options; at 12 or more, wait for typed input before showing anything (matching search-vs-browse UX conventions) — see `LOOKUP_BROWSE_THRESHOLD` in `CreateRequestForm.tsx`, meant to apply to every §6.24 lookup, not just Create Request's | Create Request — Recipient, Category |
| §6.25 | `.opt` (`.finput.opt`, `.ftextarea.opt`, `.ccode.opt`) + companion `.req` (`.finput.req`, `.ftextarea.req`, `.panel.req` on Respond to Request) | Squint-test cue: Row Tint at rest on optional/inactive fields, Ink border at rest on required fields, both white otherwise, both focusing to Focus Tint / Brand Blue like every field — the class names, not new colors. Rule, settled 2026-08-07, extended 2026-08-07: a field carrying `.opt` shows Row Tint only while empty (or, on Add Contact's Phone, while its governing state — `sendBy === 'email'` — says it isn't yet the active channel); once it holds content (or becomes active) it renders white like a required field. `.req` is the fields that are *always* white and additionally need the Ink border to read as required rather than merely optional-and-filled — added 2026-08-07 once it became clear a white required field and a white filled-optional field were indistinguishable. Applies to the country-code selector (`.ccode`) too, not just `.finput`/`.ftextarea` — same rule, same reasoning, one more selector on the same class. Reuses `--row-tint`/`--focus-tint`/`--ink`, no new token; two of the three tinted surfaces §3.3 permits plus the existing Ink text color repurposed as a border. **Scoped exception, deliberate, not an oversight**: Main Screen's search bar (Search Type, Search text) stays white-on-Band, not Row-Tinted, even though neither field is required — see decisions log, 2026-08-07, for the reasoning (the rule exists to contrast required against optional fields *within one form's completion task*; the search bar isn't a form and has no required sibling to contrast against). **`.panel.req` on Respond to Request's Done Date/Done Time was never carried into the live conversion** (2026-08-10) — the mockup's own comment already flagged that rule as unresolved ("should be conditional on Done Date equaling Due Date and a Due Time being part of the Request... not permanent"); `RequestResponseForm.tsx` instead treats both fields as ordinary optional `.opt` inputs, matching how Request Detail already treats the same two fields. The mockup file itself is unchanged | Create Request — Recipient/Due Date/Description (`.req`), Due Time/Category/Dialog (`.opt`); Create ToDo — Description (`.req`), Category/Dialog (`.opt`); Respond to Request mockup — Done Date/Done Time (`.panel.req`, not carried into the live screen — see note above); Sign In — Email (`.req`); Add Contact / Add Contact (no-contact dialog) — Name/Email/Time Zone (`.req`), Phone/country code (`.opt`); Create Free Account / Your Account — First/Last/Display Name(/Time Zone on Create Free Account) (`.req`), Phone/country code (`.opt`) |
| §6.26 | `.fieldact` (Create Request/Create ToDo) / `.panelact`+`.panelfull` (Respond to Request) — companion pair: `.finput.req`/`.ftextarea.req` | Two related fixes, both 2026-08-07, both from the same Android-width review. (1) **Stacked action row**: Add Dialog / Add Attachment move off to their own row, right-aligned, above a full-width box — replaces the earlier `.frow.top`/`.attachrow`/`.panelrow` side-by-side layout, which gave the button a fixed width and starved a full-paragraph textarea or file list of horizontal space. Costs one line of vertical space to recover most of the row's width for the box that needs it. Respond to Request has no `.form`/`.fgroup` wrapper, so its variant (`.panelact` for the button row, `.panelfull` for the panel below) carries its own `padding:0 var(--pad)` rather than inheriting it. (2) **Required-field border**: `.req` sets a resting Ink (`var(--ink)`) border instead of Input Border, so a required field is visually distinct from an optional field that's merely been filled in (both otherwise render white per §6.25's rule). Uses the existing Ink token, not a literal black — the system has no pure-black token and #1F2933 reads as black at 1px; focuses to Brand Blue like every other field. Not applied on Respond to Request, which has no bordered required inputs | Create Request — Recipient/Due Date/Description (`.req`), Dialog/Attachments (`.fieldact`); Create ToDo — Description (`.req`), Dialog/Attachments (`.fieldact`); Respond to Request — Dialog/Attachments (`.panelact`/`.panelfull`) |
| §6.28 | `.metarow`/`.mlabel`/`.mval` (reused, not new) + `.dlgtype`/`.dlghr` | **Non-modifiable values render as label:value text, never a boxed field** — owner's rule, 2026-08-08, stated directly: "consistently show non-modifiable values as text with data field titles... as is now done for Request Response" (that screen's Date:/From:/Due: meta block, unchanged since 2026-08-05, turns out to have been the right answer all along). Retires the one-day-old `.finput[readonly]`/`--locked` boxed-and-dashed variant (§6.10, SUPERSEDED above) everywhere it had already spread: Your Account's Email, Create Free Account's Email, and Request Detail's Recipient all convert to `.metarow` (`<span class="mlabel">Field:</span><span class="mval">value</span>`) — the exact component `.mlabel`/`.mval` already were on Request Response, reused rather than duplicated under a new name. The old icon+note `.lockrow` pairing (Your Account's Email explanation) drops its SVG — an icon justified *why a boxed field is disabled*; plain text next to plain text doesn't need one — but `.lockrow` itself stays, since Display Name's plain-text caption already used the no-icon form of the same class. **Dialog Detail's Kind display is the same rule taken one step further**: a chip row (Question/Answer/Comment, one marked selected) was still a *field-shaped* control even built from inert `<span>`s, and one Dialog entry has exactly one Kind — nothing for a chip row to distinguish it from. Replaced with `.dlgtype` (the Kind itself, bold, Brand Blue, doubling as its own label — "Answer:") and `.dlghr`, a plain Rule-colored horizontal line marking where the value starts. `.dlghr` is Rule-colored, not Brand Blue, on purpose: the owner's own rough reference used an underline under the word itself and asked for "any suggested formatting tweaks," floating a horizontal rule as one option — built as a full-width rule instead of a word-width underline, and left it in the neutral Rule color since `.dlgtype` already carries the color/weight emphasis; a second blue element would have had the rule doing the label's job twice | Your Account — Email; Create Free Account — Email; Request Detail — Recipient; Dialog Detail — Dialog Entry Type (`.dlgtype`/`.dlghr`) and Date/From (`.metarow`) |
| §6.30 | `.linkband`/`.linkval` | Response Link band — Request Detail's "get a link" affordance (Week 3 Day 4, 2026-08-10). Full-width Strip band under `.noticeband`: a `.btn-secondary` "Get Response Link" until `issue_request_link` returns a token, then the resulting `/r/[token]` URL (`.linkval`, monospace, wraps) plus Copy/Regenerate. Not drawn in any mockup — Day 4 was scoped as "surface the link somewhere a sender can reach it" without a design pass, so this was built plain rather than left unbuilt | Request Detail |
| §6.31 | `.donerow`/`.donenote` | Quick-Done band, 2026-08-10 — owner's ask: mark a Request Done in as few keystrokes as possible, without forcing Done/Add Dialog/Add Attachment into a mutually-exclusive chip choice (rejected by the owner himself, since they aren't mutually exclusive) or auto-filling Done Date on load (also rejected — forces anyone who only wants to add Dialog to clear it). Strip band, `.btn`-styled "Done" button, sits above the Done Date/Done Time row. Text and button disabled-state are both purely reactive to whether Done Date currently holds a value, regardless of how it got set — clicking Done or typing directly into the field land in the same state. Not yet ported into the mockup (see the Request Response screen-map entry) | Request Response |
| §6.32 | `.actlabel` (Dialog only, as of 2026-08-12 — see below) | **Simplified empty-state Dialog row**, 2026-08-11 — owner, with a pasted-in reference mockup: Create Request, Request Detail, Request Response, Response Detail, Create ToDo, and ToDo Detail all showed inconsistent empty-state treatments for Dialog and Attachments (staged-entry screens showed a bare button, existing-thread screens always rendered the full `.panelfull`/`.panel`+`.panelhead` with a "No Dialog entries yet." placeholder), and the descriptive text next to Add Dialog/Add Attachment wasn't needed once entries existed. Originally one `.frow` pairing a label with the Add button, replacing the heavier markup at zero entries: `.actlabel` (bordered box, "Questions, Answers, Comments") for Dialog — an available feature, styled like the field boxes it sits among; `.actlabel.locked` (plain muted text, "Subscription feature", no box) for Attachments — an unavailable one, kept in the existing `.is-locked` visual language. Deliberately not a §6.10-style boxed read-only value display (SUPERSEDED above) — a static section descriptor next to a button, not a value. Reverts to each screen's own pre-existing populated-state markup once entries exist; Attachments has no populated state anywhere yet (no attachment storage built), so it stays the compact row unconditionally, with Request Response/Response Detail's `owner_tier === 'subscriber'` gate unchanged around it. **Corrected 2026-08-12** (owner-reported, screenshot of Request Detail), two fixes: `.actlabel`'s Dialog text now reads "Questions, Answers, Comments `(optional)`" (`.subnote`, same treatment as Due Time/Category/Phone) and its background is Row Tint, not white — both were simply the existing §6.25 optional-field-at-rest rule not yet applied here, not a deliberate exception. And: the space between "Comments" and "(optional)" wasn't rendering at all — not a missing space in the JSX, but `.actlabel` being `display:flex` with no `gap` set, so the trailing space in the anonymous text flex item collapsed at that item's own inline-formatting-context boundary (same reason `.ffloat .flabel` needs its own explicit `gap:6px`). Fixed with `gap:4px` on `.actlabel`. **Attachments moved off `.actlabel.locked` onto `.donerow`/`.donenote` (§6.31) the same day** — owner: the centered, box-less "Subscription feature" text "is not immediately visually related to the button on the right"; asked for the standard bolded, left-aligned "Note: ..." treatment already used for the quick-Done band directly above it on these same screens. Reused `.donerow`/`.donenote` rather than inventing a second "Note:" component — `.actlabel.locked` is now removed from `app/globals.css` (orphaned once this landed). Text: "**Note:** Attachments are a Subscription feature." **Ported into the mockups the same day, once corrected** — the mockups were never actually dependent on an external, read-only `components.css` as first believed (see "Notes" below for the full correction); `WYP_create_request_palette1.html`, `WYP_create_todo_palette1.html`, `WYP_request_detail_palette1.html`, and `WYP_todo_detail_palette1.html` all now carry the `(optional)`/Row-Tint/`.donerow` fixes verbatim. Not ported into Request Response/Response Detail's mockups — both still use the older `.panel`-based Dialog/Attachments markup, a separate, already-flagged conversion gap. See decisions log, 2026-08-11 and 2026-08-12 | Create Request, Create ToDo (Dialog + Attachments, both mockup and live, with functional empty/populated JS toggle); Request Detail, ToDo Detail (Dialog + Attachments, both mockup and live — Dialog's empty state is unreachable in these two, permanently seeded with demo data, so it's documented in a comment rather than built as unreachable toggle JS); Request Response, Response Detail (Attachments/Dialog live only — mockups still on the older `.panel` pattern, not yet converted) |
| §6.33 | `.fclear` | **Clear button for Time fields**, 2026-08-11 — owner: Due/Done Time had no way to clear a set value, unlike Due/Done Date, which do (a platform affordance some browsers give `type="date"` but not `type="time"`). A small "×", styled like the existing `.attremove` (plain text button, Ink-Soft resting, Alert-Red on press), absolutely positioned at the field's right edge (the calendar/clock glyph already owns the left edge inside `.flabel`); rendered only once the field holds a value. Placed after `.flabel` in markup, never between `.finput` and `.flabel` — that adjacency is load-bearing for the floating-label selector (see CLAUDE.md). Date fields were left alone; the owner's own report frames Date as already working. Live-only, same reasoning as `openPicker` below — none of the six Dialog/Attachments-batch screens have real `type="time"` inputs in their static HTML, so there's no native field to attach a clear button to | Create Request — Due Time; Request Detail — Due Time, Done Time; Request Response — Done Time; Response Detail — Done Time |
| §6.29 | `.subempty` | Centered, small Ink-Soft note filling a `.subbody` (or a section of one) that has no rows to show and isn't a form — distinct from `.lookup-empty` (a dropdown-specific empty state) and from simply leaving the space blank, which would read as a loading glitch rather than an intentional state. First need: Main Screen's Received subcard, which has no live data source yet (see the Screen map entry above) — also reused for Sent/ToDos' own loading and error states, so all three "nothing to show yet" cases in one subcard read the same way | Main Screen — Received (deferred state), Sent/ToDos (loading/error states) |
| §6.36 | `.archrow`/`.archcheck`/`.archspacer`/`.archbody` | **Archive screen**, 2026-08-14 — owner's own drafted UI strategy, pasted-in reference screenshots for the Sent Requests and ToDos states. A per-row selection checkbox added to the *left* of the existing Sent/Received/ToDo row markup (`.row`/`.r1`/`.r2`, `.row.td`/`.t1`/`.tdc`), reusing `.checkrow`'s own checkbox sizing/color (§6.20) rather than a new one; `.archrow` is a plain flex wrapper so the checkbox sits outside the existing 4-column `.r1` grid instead of joining it as a 5th column, and `.archspacer` repeats its width once in the column-header row so labels still line up with the data below. Not yet in the UI spec — flagged here per this table's own convention (§6 occupied through §6.18 in the spec proper; the table tracks everything proposed since). **Table maintenance note**: §6.34 (Print Reports) and §6.35 (Status chip row) were never added to this table when they shipped — only mentioned inline in their own screen-map rows above. Flagged, not fixed in this pass; worth a cleanup sweep later | Archive |
| §6.27 | `.modalhead` (§6.12 variant) + `.chiprow` + `.qpicker`/`.lookup-item.selected` + `.dlgre`/`.dlgbody` | **Add Dialog modal**, 2026-08-07, replacing the old always-visible inline Dialog textarea on Create Request/Create ToDo (Respond to Request never had an inline compose field — Add Dialog there previously did nothing in the mockup). `.modalhead` is a second §6.12 header shape — title and Cancel/Save share the top row, instead of Add Category's title-then-bottom-`.modalacts` — used because that's what the owner's own mockup for this modal draws; both shapes are now valid, chosen per modal. `.chiprow` is `.sendrow`'s Strip-backdrop properties under a neutral name (a Kind picker has nothing to do with delivery channel) holding three `.chip`s — Question, Answer, Comment. **Kind availability is context-dependent, not fixed**: on Create Request/Create ToDo, Answer is always `.chip.is-locked` — a Request or ToDo starts with an empty thread, so there is never anything yet to answer; on Respond to Request (and, later, Request Detail), Answer unlocks dynamically, enabled only when at least one Question in the thread is still open (unanswered) — owner's rule, 2026-08-07: "the 'answer' could not be the 1st entry, but either a question or a comment could be." **Which-Question picker** (`.qpicker`, rows styled with `.lookup-item`/`.lookup-item.selected`, each prefixed with `.dlgwho`): shown whenever Answer is selected and at least one Question is open. Defaults to the most recently created open Question. **Superseded 2026-08-10** — originally shown only when more than one Question was open, a single open Question linking silently with no picker, per the owner's explicit scoping at the time ("it only needs to be presented if there is more than one question"); reversed after the owner found, testing Request Response live, that answering one of two open Questions and reopening Add Dialog showed nothing for the one remaining — no visual confirmation of what was about to be answered. Each row also now names who asked, deliberately not filtered to "the other party" — "a person may choose to answer their own question so only showing questions by the other party would not be appropriate." **Who-display corrected same day**: first shipped as a bold `Name:` prefix (`.qpicker-who`), then changed to `(Name)` reusing `.dlgwho` verbatim, per the owner's direct follow-up that the parenthesized form is "more consistent with the full dialog list" — `.qpicker-who` removed from `globals.css` and all three mockups. **Single-question clarifying note, added 2026-08-10**: with exactly one open Question, its row already rendered `.selected` — the identical treatment a multi-row picker uses for "the one you've clicked" — which the owner flagged as potentially reading like it still needed a click. A `.subnote` reading "(The only question is selected below.)" now appears next to the "Which Question?" label only in that single-question case, where it's true and disambiguating; it doesn't appear once there's an actual choice to make. And Add Dialog now opens with the Answer chip pre-selected whenever a Question is open, rather than always defaulting to Question. **Focus-management fix, 2026-08-10**: switching Kind chips after the modal was already open didn't return focus to the Dialog Text textarea (`autoFocus` only fires on mount) — fixed with an explicit `.focus()` call in each screen's kind-selection function. That fix then surfaced a second issue the owner caught immediately: clicking the *locked* Answer chip (Create Request/Create ToDo always; Respond to Request/Request Detail/ToDo Detail when no Question is open) still stole focus away from the textarea via the browser's native click-to-focus behavior on any real, non-natively-`disabled` button (`.is-locked` uses `aria-disabled`, not `disabled`, so screen readers can still reach the explanation — see §6.22 — which means the element stays genuinely focusable). Fixed at the source with `onMouseDown={(e) => e.preventDefault()}` on every Kind chip, locked or not, everywhere the Add Dialog modal exists — this cancels the browser's default focus-shift on mousedown without blocking the click handler itself, so clicking a locked chip is now a true no-op: no focus movement, no visual change. Needs `dialog.replies_to_id` (migration 006, confirmed run 2026-08-09) to know which Question an Answer answers — without it, "open" could only be tracked as one flag for the whole thread, which breaks with more than one Question open at once. **Display stays entry order, not re-threaded** — owner confirmed this explicitly ("That strategy would keep the 'order of entry' true"): an Answer still renders wherever it was posted in the newest-first list, not moved next to its Question. What changes is `.dlgre`/`.dlgbody`: an Answer row gets a small italic "Re: `<question, truncated>`" line (`.dlgre`, single-line ellipsis like `.attach`'s filename truncation, not the 2-line Description rule) plus a bolded body (`.dlgbody`) — the reader gets the link without the list itself moving. Bold was chosen over a new text color for the body: Brand Blue and Alert Red are both already committed to other meanings (§3.1), and reusing bold keeps this inside the emphasis language the system already has | Create Request, Create ToDo — Kind chips only (Answer permanently locked, no picker); Respond to Request — full dynamic Kind chips + which-Question picker + `.dlgre`/`.dlgbody` on Answer entries |

These were first written as §6.15–6.17, which collide with **Reserved ad slot**,
**Lookup and picker fields**, and **Calendar and clock pickers** in v2.9.
Renumbered 2026-08-03 once the spec was in the repo — the collision was
invisible while the spec lived outside it.

## Entitlements

Settled 2026-08-03.

**Rights come from the issuer, never from the reader.** A subscriber
responding to a request sent by a free user gets the free feature set. The
recipient's own status is irrelevant; they did not create the request.

**Tier lives on `profiles` and is read live.** Revised 2026-08-03. The earlier
rule snapshotted capability flags onto each request, to stop a lapse changing
the screen under a recipient. Narrowing the gate makes that unnecessary.

**Gates govern adding, not viewing.** Attachments already on a request stay
visible to everyone, permanently, whatever anyone's tier is now. Only the Add
Attachment control locks. Files are reclaimed by lapse-and-auto-delete
(PRD §6.3), which leaves a tombstone — never by hiding them from a reader.

This is why the gate note reads "Adding attachments is available on requests
sent by subscribers" rather than "Attachments are available…": the second
wording implies the files are hidden too, which they are not.

**A locked control is a courtesy, never a control.** The recipient path runs
through `SECURITY DEFINER` functions, so the function must refuse an attachment
write when the request's flags say no. Assume the button was bypassed.

See `WYP_component_states_palette1.html` for the visual treatment and the
accessibility rules that go with it.

## When a variant screen is warranted

- **A control changing state** — locked, read-only, invalid, gated — is
  documented once in the component states reference. Do not fork a screen file
  to show it; the layout is identical and the copy would drift.
- **A layout or flow difference** — an empty state, a dialog, a first-run
  variant that removes actions — gets its own screen file, because it cannot be
  read off a component gallery.

Rule of thumb: if the difference is "that control is in state X", it belongs in
the reference. If it is "the screen shows something else", it needs a file.

Where a screen has two meaningful modes that share a layout — Sign In's join and
returning modes, Request Response's free and subscriber tiers — put both in
one file behind a preview toggle rather than in two files.

## Notes

`drafts/WYP_requests_partial_SUPERSEDED.html` was built from a partial
screenshot before the full main screen existed. It shows only one Sent card and
no ToDos section. Kept only to explain the Log Out placement discussion; use
`screens/WYP_main_screen_palette1.html` instead.

**Stylesheets organized and realigned (2026-08-12), correcting the entry
that used to be here.** The previous version of this note (and what the
owner was told directly, in the same conversation) claimed the mockups
"link a shared `components.css`... read-only from this session," so the
2026-08-12 `.actlabel`/`.donerow` fixes could only land in the live app, not
in any mockup. **That was wrong**, caught on re-verification when the owner
asked to organize the stylesheets: none of the 17 `design/screens/*.html`
files actually have a working `<link rel="stylesheet" href="tokens.css">` or
`href="components.css">` tag. Every apparent match was a docstring pasted
inside a mockup's own embedded `<style>` comment (e.g. "ORDER OF STYLESHEETS
on every screen: ... 2. tokens.css ... 3. components.css ...", describing a
hypothetical future build, not an active dependency) — confirmed precisely
by checking only `<link>` tags appearing before each file's first `<style>`
tag. **All 17 mockups are fully self-contained**: each embeds its own
complete `:root` token block plus component CSS directly in its own
`<style>` tag(s), exactly as `WYP_create_free_account_palette1.html` was
already known to do. The real (stale, "Draft v0.1 · July 2026") `tokens.css`/
`components.css` files live only in the Would You Please Claude.ai project's
own knowledge base — never linked by any mockup, just unused drafts.

Regenerated both as `design/screens/tokens.css` (49 lines, the `:root` block)
and `design/screens/components.css` (2,100+ lines) from `app/globals.css` —
the live app's stylesheet, which turns out to be the thing that's actually
been kept current every session, even though the header comment in every
mockup still claims the opposite direction ("Source of truth: ... Keep in
sync with the mockups"). These two files exist as the current, correct
*reference* for keeping each mockup's own embedded copy in sync by hand —
not as something any mockup links to today, and not changed to become one
(converting to real shared `<link>`s would be a bigger, separately-decided
architectural change, and would cost each mockup the "open directly from
disk, no missing assets" property the Conventions section above calls out).

Audited all 17 mockups' embedded CSS against the new canonical copy: token
values matched everywhere they existed (zero conflicts — only expected gaps
for tokens newer than a given mockup, like `--locked`/`--focus-halo`). Class
coverage found five mockups genuinely missing rules for classes their own
markup already used — real, pre-existing staleness bugs, not all created by
this session's `.actlabel` work:

- `WYP_create_request_palette1.html` — `.actlabel`, `.dlgstaged`, `.ferror`
  were referenced but undefined; separately, `.chip`/`.chip.selected` (the
  Add Dialog Kind picker's own base button styling) turned out to be missing
  entirely, so Question/Comment rendered as unstyled native buttons — not
  caught by the original audit pass, since `.chip.is-locked` being present
  made a naive per-token check think bare `.chip` was too. Found by manual
  cross-check against the five sibling mockups, all of which had it.
- `WYP_create_todo_palette1.html` — `.actlabel`/`.ferror` undefined; more
  notably, `.donerow`/`.donenote` were used by the (already-shipped) quick-Done
  band but never defined anywhere in the file, so that band has been
  rendering completely unstyled since it was added 2026-08-10.
- `WYP_request_detail_palette1.html`, `WYP_todo_detail_palette1.html` —
  `.donerow`/`.donenote` added for the Attachments conversion below (neither
  had a quick-Done band to also fix).
- `WYP_respond_to_request_palette1.html` — `.subnote` (used by the which-
  Question picker's note) was undefined.

Ported the 2026-08-12 Dialog/Attachments fix into the four mockups that use
the current `.actlabel` pattern (`WYP_create_request_palette1.html`,
`WYP_create_todo_palette1.html`, `WYP_request_detail_palette1.html`,
`WYP_todo_detail_palette1.html`): Dialog's empty-state row now reads
"Questions, Answers, Comments `(optional)`" and Attachments' locked row is
now `.donerow`/`.donenote` ("**Note:** Attachments are a Subscription
feature.") instead of the retired `.actlabel.locked`, matching the live app
exactly. **Not** ported into `WYP_respond_to_request_palette1.html` or
`WYP_response_detail_palette1.html` — both still use the older `.panel`-based
Dialog/Attachments markup (already flagged, separately, as not yet converted
to `.actlabel` at all); bringing those two forward is a markup port, not a
stylesheet-alignment fix, and stays out of this batch's scope.

`WYP_main_screen_palette1.html`'s `c-cat`/`c-nm`/`c-pri` column classes were
also flagged by the initial audit pass as "missing" — checked and confirmed
**not a bug**: `app/globals.css` itself only ever styles `.colbar .c-dt`/
`.c-due`/`.c-dn` (the date columns, which need centering); the name/
priority/category columns were never meant to carry their own rule, relying
on the grid's own default alignment. The mockup matches the live app
exactly here.
